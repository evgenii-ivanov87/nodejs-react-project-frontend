const routes = {
  register: '/register',
  login: '/',
  projects: '/projects',
  projectDetails: '/projects/:projectId/sprints',
  sprint: '/projects/:projectId/:sprintId',
};
export default routes;
